# Data generation config for 280-dimensional system with 50% krylov strategy
# Usage: uv run python generate_data.py data-configs/generate-280-krylov50.toml
# Output: generate-280-krylov50-norm/

[flow]

[source]
matrix_path = "/data/projects/graph-cg/data/raw/matrix-280.txt"
rhs_path = "/data/projects/graph-cg/data/raw/rhs-280.txt"

[generation]
# Number of samples to generate
num_samples = 3000

# Normalization method: "spectral", "matrix", "rhs", or "none"
# - "matrix": normalizes by spectral radius bound
# - "rhs": normalizes each (A, b) pair by ||b||
# - "none": no normalization
normalize = "spectral"

# CG iterations for krylov strategy
krylov_iters = 15
# Optional CG iterations recorded for residual traces (used with cg_residual/residual)
# residual_iters = 12

# Random seed for reproducibility
seed = 42

# Shuffle final dataset
shuffle = true

[[generation.strategy]]
name = "normal"
percentage = 0.5

[[generation.strategy]]
name = "krylov"
percentage = 0.5
krylov_iters = 15

[output]
# Base directory for processed data
processed_dir = "/data/projects/graph-cg/data/processed"
# Actual output will be: {processed_dir}/generate-280-krylov50-norm/
